package ci.monprofperso.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Palette MonProfPerso, extraite fidèlement de la maquette.
 */
object AkColors {
    // Verts (couleur de marque)
    val Green = Color(0xFF0E5A43)
    val GreenDark = Color(0xFF0B4937)
    val GreenLine = Color(0xFF1C6A52)
    val GreenSoft = Color(0xFFE2EFE9)   // pastille / fonds doux verts

    // Oranges (accent)
    val Orange = Color(0xFFE8722A)
    val OrangeLight = Color(0xFFF2A04D)
    val OrangeSoft = Color(0xFFFBE9DA)  // fond doux orange

    // Fonds crème
    val Cream = Color(0xFFECE7DE)       // fond de la page maquette
    val Canvas = Color(0xFFF7F4EE)      // fond écran clair
    val CardField = Color(0xFFF2EFE8)   // champs / chips neutres
    val Border = Color(0xFFECE7DF)
    val BorderSoft = Color(0xFFEFEAE2)

    // Textes
    val Ink = Color(0xFF18241E)         // titres
    val InkSoft = Color(0xFF384640)
    val Muted = Color(0xFF67756D)       // texte secondaire
    val Faint = Color(0xFF9AA8A0)       // placeholders / inactifs
    val OnGreenSoft = Color(0xFFC6DCD2) // texte clair sur vert
    val OnGreenFaint = Color(0xFFA9C7BB)

    val White = Color(0xFFFFFFFF)
    val PhoneBezel = Color(0xFF14110E)
    val Star = Color(0xFFF2A04D)
}
